- name: Install check-jsonschema
        run: pip install check-jsonschema
      
      - name: Validate state examples
        run: |
          check-jsonschema --schemafile schemas/state.schema.json examples/state-initialization.json
      
      - name: Validate event examples  
        run: |
          check-jsonschema --schemafile schemas/event.schema.json examples/event-sequence.json
